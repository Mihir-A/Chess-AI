name: Build Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-release:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release -j 4

      - name: Setup Emscripten (Linux only)
        if: runner.os == 'Linux'
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: 3.1.64

      - name: Build WASM (Linux only)
        if: runner.os == 'Linux'
        run: |
          emcmake cmake -S . -B build-wasm -DCMAKE_BUILD_TYPE=Release
          cmake --build build-wasm -j 4

      - name: Package (macOS/Linux)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist/chess
          cp -R assets dist/chess/
          cp build/release/chess dist/chess/
          cd dist
          zip -r chess-${{ runner.os }}-release.zip chess

      - name: Package WASM (Linux only)
        if: runner.os == 'Linux'
        run: |
          mkdir -p dist/chess-wasm
          cp build/wasm/chess.js dist/chess-wasm/
          cp build/wasm/chess.wasm dist/chess-wasm/
          cp build/wasm/chess.data dist/chess-wasm/
          cd dist
          zip -r chess-wasm-release.zip chess-wasm

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist/chess | Out-Null
          Copy-Item -Recurse assets dist/chess/
          Copy-Item build/release/chess.exe dist/chess/
          Compress-Archive -Path dist/chess -DestinationPath dist/chess-Windows-release.zip

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.zip
